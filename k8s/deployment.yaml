apiVersion: apps/v1
kind: Deployment
metadata:
  name: fcs-web
  namespace: fcs-web
  labels:
    app.kubernetes.io/name: fcs-web
    app.kubernetes.io/part-of: fcs
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fcs-web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fcs-web
        app.kubernetes.io/part-of: fcs
        tags.datadoghq.com/service: fcs-web
        tags.datadoghq.com/env: production
      annotations:
        fcs.platform/deployed-at: not-deployed
        fcs.platform/source-sha: not-deployed
        fcs.platform/image: not-deployed
        ad.datadoghq.com/web.checks: |
          {
            "http_check": {
              "init_config": {},
              "instances": [{"name": "fcs-web-health", "url": "http://%%host%%:%%port%%/health", "timeout": 5, "collect_response_time": true}]
            }
          }
    spec:
      containers:
        - name: web
          image: ghcr.io/group10-tc-01/fcs-web:main
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          env:
            - name: Deployment__DeployedAt
              valueFrom:
                fieldRef:
                  fieldPath: metadata.annotations['fcs.platform/deployed-at']
            - name: Deployment__SourceSha
              valueFrom:
                fieldRef:
                  fieldPath: metadata.annotations['fcs.platform/source-sha']
            - name: Deployment__Image
              valueFrom:
                fieldRef:
                  fieldPath: metadata.annotations['fcs.platform/image']
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 12
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
